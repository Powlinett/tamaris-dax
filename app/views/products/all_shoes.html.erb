<ul>
  <% @shoes.each do |shoe| %>
    <li>
      <%= link_to shoe.reference, product_path(shoe) %>
      <%= shoe.model %>
      <% if shoe.photos.exists? %>
        <%= image_tag shoe.photos.first.url, width: '150' %>
      <% end %>
    </li>
  <% end %>
</ul>
