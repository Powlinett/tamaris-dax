<%= @product_model.model %>
<%= @product_model.reference %>

<% @photos_urls.each do |photo| %>
  <%= image_tag photo, width: '200' %>
<% end %>

<ul>
  <% @all_sizes.each do |product| %>
    <li>
      <%= link_to product.size, new_booking_path %> (<%= product.stock %> en stock)
    </li>
  <% end %>
</ul>

